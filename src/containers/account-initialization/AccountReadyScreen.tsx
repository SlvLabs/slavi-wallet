import {SafeAreaView, Text} from 'react-native';
import React from 'react';

const AccountReadyScreen = () => {
  return (
    <SafeAreaView>
      <Text>Account ready screen</Text>
    </SafeAreaView>
  );
};

export default AccountReadyScreen;
