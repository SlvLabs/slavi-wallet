import React from 'react';
import {SafeAreaView} from 'react-native';
import TokenAddingForm from '../token/token-adding-form';

const TokenAddingScreen = () => {
  return (
    <SafeAreaView>
      <TokenAddingForm />
    </SafeAreaView>
  );
};

export default TokenAddingScreen;
