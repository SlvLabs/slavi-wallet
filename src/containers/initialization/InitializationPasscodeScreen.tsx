import {SafeAreaView, Text} from 'react-native';
import React from 'react';

const InitializationPasscodeScreen = () => {
  return (
    <SafeAreaView>
      <Text>Initialization passcode screen</Text>
    </SafeAreaView>
  );
};

export default InitializationPasscodeScreen;
