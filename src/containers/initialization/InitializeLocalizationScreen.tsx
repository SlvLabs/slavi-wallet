import {SafeAreaView, Text} from 'react-native';
import React from 'react';

const InitializeLocalizationScreen = () => {
  return (
    <SafeAreaView>
      <Text>Initialization localization screen</Text>
    </SafeAreaView>
  );
};

export default InitializeLocalizationScreen;
